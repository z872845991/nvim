return {
    {
        -- Change file permissions by :SudaWrite and :SudaRead
        "lambdalisue/vim-suda",
        lazy = false,
        config = function()
        end
    },
    {
        -- :WordCount summarises the word count of the current file
        -- :WordCountLine summarises the word count of the current line
        "jspringyc/vim-word",
        lazy = false,
        config = function()
        end
    }
}